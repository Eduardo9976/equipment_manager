<% if @manufacturer.errors.full_messages.any? %>
  <p> Não foi possível salvar, verifique os erros abaixo:</p>
  <% @manufacturer.errors.full_messages.each do |erro| %>
      <p> <%= erro %> </p>
  <% end %> 
  <% end %>    


<%= form_for( @manufacturer, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field(:name) %>
  <%= f.submit('Enviar') %>
<% end %>