<%if @product.errors.full_messages.any? %>
  <p> Não foi possivel salvar, verifique os erros abaixo </p>
  <%@product.errors.full_messages.each do |erro| %>
    <p> <%=erro %> </p>
  <%end%>
<%end%>    

<%= form_with( model: @product, local:true ) do |f| %>
  <%= f.label :name, 'Nome'%>
  <%= f.text_field(:name)%>
  <%= f.label :manufacturer_id, 'Fabricante:'%>
  <%= f.collection_select :manufacturer_id, @manufacturer, :id, :name%>
  <%= f.submit('Enviar') %>
<% end %>

